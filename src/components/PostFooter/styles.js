import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  footer: {
    flexDirection: "row",
    alignItems: "center",
    padding: 10,
  },
  heart: {
    marginRight: 10,
    width: 35,
    height: 35,
  },
});

export default styles;
