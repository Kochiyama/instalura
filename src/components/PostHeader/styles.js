import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  postHeader: {
    padding: 10,
    flexDirection: "row",
    alignItems: "center",
  },
  image: {
    width: 40,
    height: 40,
    borderRadius: 50,
  },
  username: {
    marginLeft: 10,
    fontSize: 16,
  },
});

export default styles;
